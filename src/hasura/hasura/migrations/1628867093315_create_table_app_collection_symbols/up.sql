CREATE TABLE "app"."collection_symbols" ("id" serial NOT NULL, "collection_id" integer NOT NULL, "symbol" varchar NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("collection_id") REFERENCES "app"."collections"("id") ON UPDATE cascade ON DELETE cascade);
