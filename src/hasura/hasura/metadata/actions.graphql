type Query {
  fetchChartData (
    symbol: String!
    period: String!
  ): [ChartDataPoint]
}

type ChartDataPoint {
  datetime : String
  open : Float
  high : Float
  low : Float
  close : Float
  volume : Float
}

