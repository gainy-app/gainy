- table:
    schema: app
    name: categories
  array_relationships:
  - name: profile_categories
    using:
      foreign_key_constraint_on:
        column: category_id
        table:
          schema: app
          name: profile_categories
  select_permissions:
  - role: anonymous
    permission:
      columns:
      - id
      - name
      filter: {}
  - role: user
    permission:
      columns:
      - id
      - name
      filter: {}
- table:
    schema: app
    name: interests
  array_relationships:
  - name: profile_interests
    using:
      foreign_key_constraint_on:
        column: interest_id
        table:
          schema: app
          name: profile_interests
  select_permissions:
  - role: anonymous
    permission:
      columns:
      - id
      - name
      filter: {}
- table:
    schema: app
    name: profile_categories
  object_relationships:
  - name: category
    using:
      foreign_key_constraint_on: category_id
  - name: profile
    using:
      foreign_key_constraint_on: profile_id
- table:
    schema: app
    name: profile_interests
  object_relationships:
  - name: interest
    using:
      foreign_key_constraint_on: interest_id
  - name: profile
    using:
      foreign_key_constraint_on: profile_id
- table:
    schema: app
    name: profiles
  array_relationships:
  - name: profile_categories
    using:
      foreign_key_constraint_on:
        column: profile_id
        table:
          schema: app
          name: profile_categories
  - name: profile_interests
    using:
      foreign_key_constraint_on:
        column: profile_id
        table:
          schema: app
          name: profile_interests
  insert_permissions:
  - role: user
    permission:
      check:
        user_id:
          _eq: X-Hasura-User-Id
      columns:
      - email
      - first_name
      - gender
      - investment_experience
      - investment_goals
      - investment_horizon
      - last_name
      - risk_level
      backend_only: false
  select_permissions:
  - role: user
    permission:
      columns:
      - email
      - first_name
      - investment_experience
      - last_name
      - id
      - investment_goals
      - investment_horizon
      - risk_level
      - gender
      - user_id
      - created_at
      filter:
        user_id:
          _eq: X-Hasura-User-Id
  update_permissions:
  - role: user
    permission:
      columns:
      - first_name
      - gender
      - investment_experience
      - investment_goals
      - investment_horizon
      - last_name
      - risk_level
      filter:
        user_id:
          _eq: X-Hasura-User-Id
      check:
        user_id:
          _eq: X-Hasura-User-Id
- table:
    schema: tap_eodhistoricaldata
    name: fundamentals
